<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,200;0,300;0,500;0,700;1,200;1,300;1,500;1,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/reset.css">
    <link rel="stylesheet" href="css/style2.css">
    <title>CoachHer</title>
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a class="navLinks" href="/">Home</a></li>
        <li><a class="navLinks" href="/browsecoaches">Browse Coaches</a></li>
        <li><a class="navLinks" href="/signup">Sign Up</a></li>
        <li><a class="navLinks" href="/login">Log In</a></li>
        <li><a class="navLinks" href="/">Log Out</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <!-- profile pic goes here -->
    <section class="profilePic">
      <h1><%= user.local.username%>'s Profile</h1>
    <!-- <img src="" alt="">
      <% if (user.local.email) { %>
      <form action="/profilePic" method="POST" enctype="multipart/form-data">
        <input class="postImage" type="file" name="file-to-upload" placeholder="Upload Image">
        <button type="submit">Add Photo</button>
      </form>
      <% } %>
 -->


    </section>

    <section class="profileDisplay">
      <!-- <section>
        <p>Off/On Season</p>
        <label class="switch">
          <input type="checkbox">
          <span class="slider round"></span>
        </label>
      </section> -->


      <section>
        <h2>Chat with a Coach</h2>
        <ul>
            <% for(var i=0; i<coaches.length; i++) {%>
              <li class="message">
                <!-- <span><%= coaches[i].local.username %></span> <a href="/chatRequest?coachID=<%= coaches[i]._id %>"><button type="button" name="button">Chat with <%= coaches[i].local.username %></button></a> -->
                <form class="" action="/chatRequest" method="post">
                  <span><%= coaches[i].local.firstname %></span> <span><%= coaches[i].local.lastname %></span>
                  <input type="hidden" name="coachID" value="<%= coaches[i]._id %>"><button type="submit" name="button">Request to chat with <%= coaches[i].local.username %></button>
                  <a href="/room/:room">Chat with <%=coaches[i].local.lastname %></a>
                  <!-- use  array find to search through the request arrays with any of the coaches-->
                </form>

              </li>
            <% } %>
            </ul>
      </section>
    </section>
<!-- Browse coaches section -->


  </main>

  <footer>
    <p class="copy"> Copyright © 2021 CoachHer.All rights reserved.</p>
  </footer>
    <script src="js/main.js"></script>
</body>
</html>
